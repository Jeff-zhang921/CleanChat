# API / socket endpoints
VITE_API_URL="/api"
VITE_SOCKET_URL="/"

# Optional: frontend-side VAPID public key override.
# If omitted, frontend will fetch from backend /profile/push/public-key.
# Never place VAPID_PRIVATE_KEY in frontend env.
VITE_VAPID_PUBLIC_KEY="base64url_public_key"
